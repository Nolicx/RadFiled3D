[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

# [project]
# name = "RadFiled3D"
# version = "0.0.0"
# description = "RadFiled3D"
# readme = "README.md"
# requires-python = ">=3.12"